"""Operations bounded context.

Owns trip orders (đơn hàng), work orders (phiếu làm việc), and the
many-to-many BookedTrip ↔ DeliveredTrip reconciliation join. Imports of
customer Excel files end here as fully-formed BookedTrips.
"""
