# Runtime dependencies only — used by Dockerfile
# Web framework
fastapi==0.136.3
uvicorn[standard]==0.30.6

# Database
sqlalchemy==2.0.35
alembic==1.13.2
asyncpg==0.29.0
psycopg2-binary==2.9.9

# Auth
PyJWT[cryptography]==2.12.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.27
itsdangerous==2.2.0

# Validation
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0

# Utilities
python-dotenv==1.2.2
httpx==0.27.2

# Redis & background jobs
redis[hiredis]>=5.0.0
arq>=0.26.0

# Push notifications
pywebpush>=2.0

# CORS & middleware
starlette==1.0.1

# Date/time
python-dateutil==2.9.0

# Image processing (OCR preprocessing)
Pillow>=10.0.0
# HEIC/HEIF decoding — iOS photos arrive as HEIC and must be transcoded to JPEG
pillow-heif>=0.18.0

# Excel processing
openpyxl>=3.1.0
xlrd>=2.0.0

# Fuzzy matching
Levenshtein>=0.27.0

# Monitoring
sentry-sdk==2.13.0
prometheus-fastapi-instrumentator==8.0.0
